import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ProfileUserData = () => {
  return (
    <View>
      <Text>ProfileUserData</Text>
    </View>
  );
};

export default ProfileUserData;

const styles = StyleSheet.create({});
